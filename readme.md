# `git-wasm`

This is a fork of [`wasm-git`](https://github.com/petersalomonsen/wasm-git) to merge new features from other forks, and further develop the library enough to replace the use of the `git` CLI in some contexts.
